---
- name: reload exports
  ansible.builtin.command:
    cmd: exportfs -ra
  changed_when: false

- name: restart nfs
  ansible.builtin.systemd:
    name: nfs-server
    state: restarted
    daemon_reload: true

- name: restart sshd
  ansible.builtin.systemd:
    name: ssh
    state: restarted
